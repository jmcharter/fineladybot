name: Test
on:
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Test with pytest
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        pytest